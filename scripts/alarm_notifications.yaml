alarm_notification:
  sequence:
    - condition: state
      entity_id: input_boolean.disable_alarm_notification
      state: 'off'
    - service: notify.tim_phone_notifier
      data:
        title: "{{ title }}"
        message: "{{ message }}"