- alias: 'Turn on Hallway when storage sensor triggered'
  trigger:
    - platform: state
      entity_id: binary_sensor.motion_sensor_storage_2
      to: 'on'
  condition:
    condition: and
    conditions:
      - condition: template
        value_template: '{{ is_state_attr("sensor.daylight", "daylight", false) }}'
      - condition: state
        entity_id: input_boolean.no_one_home
        state: 'off'
      - condition: state
        entity_id: input_boolean.asleep
        state: 'off'
  action:
    - service: light.turn_on
      entity_id: light.hue_bulb_hallway

- alias: 'Turn off Hallway after 3 min - Storage trigger'
  trigger:
    - platform: state
      entity_id: binary_sensor.motion_storage_2
      to: 'off'
      for:
        minutes: 3
  action:
    - service: light.turn_off
      entity_id: light.hue_bulb_hallway
